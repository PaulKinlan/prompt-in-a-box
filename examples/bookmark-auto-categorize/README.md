# bookmark-auto-categorize

Schedule-driven. Finds loose bookmarks (directly in Bookmarks Bar / Other Bookmarks), classifies them into one of 10 categories via LLM, moves them into named subfolders.

- **Trigger:** alarm
- **Required permissions:** `bookmarks`, `notifications`
- **Side effects:** creates folders, moves bookmarks (never deletes)
