# auto-group-by-domain

Schedule-driven. Groups tabs into collapsed tab groups by registered domain when there are 3+ tabs from the same domain in a window.

- **Trigger:** alarm
- **Required permissions:** `tabs` (tab groups need no extra permission)
- **Side effects:** creates/extends tab groups; never closes tabs
